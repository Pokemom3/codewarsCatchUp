function makeNegative(num) {
  //can use the -Maths.abs
  // * positive num by -1
  // if num is positive so more than 0 return -num

  if (num > 0) {
    return -num;
  } else {
    return num;
  }
}

//return -Math.abs(num);

//return -num;
